"use client";

import { useBabyProfile } from "@/components/baby/baby-profile-provider";
import { PiluIllustration } from "@/components/illustrations/pilu-illustration";
import { useLibrary } from "@/components/library/library-provider";
import { ageFilters, categoryLabels, libraryArticles, matchesAge, searchArticles } from "@/lib/library-data";
import type { AgeRange, Article, ArticleCategory } from "@/types/library";
import { Apple, Baby, BedDouble, BookMarked, ChevronLeft, Clock3, Heart, HeartHandshake, Lightbulb, Milk, Moon, ShieldCheck, Sparkles, Sprout, Stethoscope, Tag, TentTree, Toilet, Volume2, X } from "lucide-react";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";

const categoryIcons: Record<ArticleCategory, typeof Baby> = { newborn: Baby, feeding: Milk, sleep: Moon, crying: Volume2, diapers: Toilet, health: Stethoscope, safety: ShieldCheck, development: Sprout, teething: Apple, solidFoods: Apple, parentWellbeing: HeartHandshake, agesOneToFour: TentTree };
const popularTopics = [{ label: "Colic and gas", query: "crying" }, { label: "Sleep regression", query: "sleep" }, { label: "Bottle feeding", query: "bottle" }, { label: "Diaper changes", query: "diapers" }, { label: "Teething", query: "teething" }, { label: "Tummy time", query: "tummy" }];

export function LibraryDashboard() {
  const { bookmarks, history, isBookmarked, toggleBookmark } = useLibrary(); const { profile } = useBabyProfile();
  const [query, setQuery] = useState(""); const [category, setCategory] = useState<ArticleCategory | "all" | "saved">("all"); const [age, setAge] = useState<AgeRange>("All ages");
  const filtered = useMemo(() => { let articles = searchArticles(libraryArticles, query).filter((article) => matchesAge(article, age)); if (category === "saved") articles = articles.filter((article) => isBookmarked(article.slug)); else if (category !== "all") articles = articles.filter((article) => article.category === category); return articles; }, [query, category, age, isBookmarked]);
  const featured = libraryArticles.find((article) => article.featured) ?? libraryArticles[0]; const saved = libraryArticles.filter((article) => isBookmarked(article.slug)); const recentlyRead = history.map((item) => libraryArticles.find((article) => article.slug === item.articleSlug)).filter((article): article is Article => Boolean(article));
  const babyAge: AgeRange = profile.dateOfBirth ? "0–3 months" : "All ages"; const relevant = libraryArticles.filter((article) => matchesAge(article, babyAge)).slice(0, 3);
  return <div className="library-page"><LibraryHeader /><ArticleSearch value={query} onChange={setQuery} onClear={() => setQuery("")} resultCount={filtered.length} /><LibraryCategoryChips active={category} onChange={setCategory} /><AgeRangeFilter active={age} onChange={setAge} />
    {!query && category === "all" && age === "All ages" ? <><FeaturedArticleCard article={featured} bookmarked={isBookmarked(featured.slug)} onBookmark={() => toggleBookmark(featured.slug)} /><PopularTopics onPick={setQuery} /><ArticleSection title={`Relevant for ${profile.preferredName}`} subtitle="Selected by age range only" articles={relevant} isBookmarked={isBookmarked} onBookmark={toggleBookmark} /><SavedArticlesSection articles={saved} isBookmarked={isBookmarked} onBookmark={toggleBookmark} /><RecentlyReadSection articles={recentlyRead} isBookmarked={isBookmarked} onBookmark={toggleBookmark} /><ArticleSection title="Latest articles" subtitle="Gentle ideas for everyday moments" articles={libraryArticles.filter((article) => article.slug !== featured.slug).slice(0, 6)} isBookmarked={isBookmarked} onBookmark={toggleBookmark} /></> : <SearchResults articles={filtered} query={query} category={category} isBookmarked={isBookmarked} onBookmark={toggleBookmark} />}
    <p className="library-disclaimer">Pilu Library provides general educational information and does not replace advice from a qualified healthcare professional.</p>
  </div>;
}

export function LibraryHeader() { return <header className="library-header"><div><p>Simple guidance for every new stage</p><h1>Baby Library</h1><span>Short, calm reads for the little moments.</span></div><BookMarked size={29} aria-hidden="true" /></header>; }
export function ArticleSearch({ value, onChange, onClear, resultCount }: { value: string; onChange: (value: string) => void; onClear: () => void; resultCount: number }) { return <div className="article-search"><label htmlFor="library-search" className="sr-only">Search Baby Library</label><Lightbulb size={18} aria-hidden="true" /><input id="library-search" value={value} onChange={(event) => onChange(event.target.value)} placeholder="Search a topic, question or stage" /><span role="status" aria-live="polite">{value ? `${resultCount} results` : ""}</span>{value ? <button type="button" onClick={onClear} aria-label="Clear library search"><X size={17} /></button> : null}</div>; }
export function LibraryCategoryChips({ active, onChange }: { active: ArticleCategory | "all" | "saved"; onChange: (category: ArticleCategory | "all" | "saved") => void }) { return <div className="library-chips" role="tablist" aria-label="Article categories">{(["all", "saved", ...Object.keys(categoryLabels)] as (ArticleCategory | "all" | "saved")[]).map((category) => { const Icon = category === "saved" ? BookMarked : category === "all" ? Sparkles : categoryIcons[category]; const label = category === "all" ? "All" : category === "saved" ? "Saved" : categoryLabels[category]; return <button key={category} type="button" role="tab" aria-selected={active === category} className={active === category ? "library-chips__button library-chips__button--active" : "library-chips__button"} onClick={() => onChange(category)}><Icon size={15} aria-hidden="true" />{label}</button>; })}</div>; }
export function AgeRangeFilter({ active, onChange }: { active: AgeRange; onChange: (age: AgeRange) => void }) { return <div className="age-range-filter" role="group" aria-label="Filter articles by age">{ageFilters.map((age) => <button type="button" key={age} className={active === age ? "age-range-filter__button age-range-filter__button--active" : "age-range-filter__button"} onClick={() => onChange(age)}>{age}</button>)}</div>; }
export function FeaturedArticleCard({ article, bookmarked, onBookmark }: { article: Article; bookmarked: boolean; onBookmark: () => void }) { return <article className="featured-article"><div><span>Featured read</span><h2>{article.title}</h2><p>{article.summary}</p><small>{article.ageRange} · {article.readingTime}</small><Link href={`/library/${article.slug}`}>Read the guide</Link></div><PiluIllustration variant={article.illustration} alt="Gentle Pilu baby illustration" /><BookmarkButton article={article} active={bookmarked} onClick={onBookmark} /></article>; }
export function PopularTopics({ onPick }: { onPick: (query: string) => void }) { return <section className="popular-topics"><header><p>Popular topics</p><h2>Start with a familiar question</h2></header><div>{popularTopics.map((topic) => <button key={topic.label} type="button" onClick={() => onPick(topic.query)}><Tag size={15} aria-hidden="true" />{topic.label}</button>)}</div></section>; }
export function ArticleSection({ title, subtitle, articles, isBookmarked, onBookmark }: { title: string; subtitle: string; articles: Article[]; isBookmarked: (slug: string) => boolean; onBookmark: (slug: string) => void }) { return <section className="library-section"><header><div><p>{subtitle}</p><h2>{title}</h2></div></header>{articles.length ? <ArticleGrid articles={articles} isBookmarked={isBookmarked} onBookmark={onBookmark} /> : <LibraryEmptyState kind="articles" />}</section>; }
export function ArticleGrid({ articles, isBookmarked, onBookmark }: { articles: Article[]; isBookmarked: (slug: string) => boolean; onBookmark: (slug: string) => void }) { return <div className="article-grid">{articles.map((article) => <ArticleCard key={article.slug} article={article} bookmarked={isBookmarked(article.slug)} onBookmark={() => onBookmark(article.slug)} />)}</div>; }
export function ArticleCard({ article, bookmarked, onBookmark }: { article: Article; bookmarked: boolean; onBookmark: () => void }) { const Icon = categoryIcons[article.category]; return <article className="article-card"><Link href={`/library/${article.slug}`} className="article-card__link"><div className="article-card__illustration"><PiluIllustration variant={article.illustration} alt="" /><Icon size={16} aria-hidden="true" /></div><div><span>{categoryLabels[article.category]}</span><h3>{article.title}</h3><p>{article.summary}</p><small>{article.ageRange} · <Clock3 size={13} aria-hidden="true" />{article.readingTime}</small></div></Link><BookmarkButton article={article} active={bookmarked} onClick={onBookmark} /></article>; }
export function BookmarkButton({ article, active, onClick }: { article: Article; active: boolean; onClick: () => void }) { return <button type="button" className={active ? "bookmark-button bookmark-button--active" : "bookmark-button"} aria-label={active ? `Remove ${article.title} from saved articles` : `Save ${article.title}`} aria-pressed={active} onClick={onClick}><BookMarked size={17} fill={active ? "currentColor" : "none"} aria-hidden="true" /></button>; }
export function SavedArticlesSection({ articles, isBookmarked, onBookmark }: { articles: Article[]; isBookmarked: (slug: string) => boolean; onBookmark: (slug: string) => void }) { return <section className="library-section"><header><div><p>Kept for later</p><h2>Saved articles</h2></div></header>{articles.length ? <ArticleGrid articles={articles.slice(0, 4)} isBookmarked={isBookmarked} onBookmark={onBookmark} /> : <LibraryEmptyState kind="saved" />}</section>; }
export function RecentlyReadSection({ articles, isBookmarked, onBookmark }: { articles: Article[]; isBookmarked: (slug: string) => boolean; onBookmark: (slug: string) => void }) { return <section className="library-section"><header><div><p>Your local session</p><h2>Recently read</h2></div></header>{articles.length ? <ArticleGrid articles={articles} isBookmarked={isBookmarked} onBookmark={onBookmark} /> : <LibraryEmptyState kind="recent" />}</section>; }
export function SearchResults({ articles, query, category, isBookmarked, onBookmark }: { articles: Article[]; query: string; category: ArticleCategory | "all" | "saved"; isBookmarked: (slug: string) => boolean; onBookmark: (slug: string) => void }) { return <section className="library-section"><header><div><p>{query ? `Results for “${query}”` : category === "saved" ? "Saved reads" : category === "all" ? "All ages" : categoryLabels[category]}</p><h2>{articles.length ? "Helpful reads" : "Nothing here yet"}</h2></div></header>{articles.length ? <ArticleGrid articles={articles} isBookmarked={isBookmarked} onBookmark={onBookmark} /> : <LibraryEmptyState kind={category === "saved" ? "saved" : "search"} />}</section>; }
export function LibraryEmptyState({ kind }: { kind: "search" | "saved" | "recent" | "articles" }) { const copy = { search: ["No matches yet", "Try a different word, topic or age range."], saved: ["No saved articles", "Save a guide to come back to it later."], recent: ["No recently read articles", "The guides you open in this session will appear here."], articles: ["No articles in this view", "Choose another category or age range."] }[kind]; return <div className="library-empty"><PiluIllustration variant="teddy" alt="" /><div><p>{copy[0]}</p><span>{copy[1]}</span></div></div>; }

export function ArticleReader({ article }: { article: Article }) { const { isBookmarked, toggleBookmark, recordRead, feedback, setFeedback } = useLibrary(); const { profile } = useBabyProfile(); useEffect(() => { recordRead(article.slug); }, [article.slug, recordRead]); const related = article.relatedSlugs.map((slug) => libraryArticles.find((item) => item.slug === slug)).filter((item): item is Article => Boolean(item)); const question = `I was reading about ${article.title.toLowerCase()}. Can you explain the common gentle steps for ${profile.preferredName}?`; return <article className="article-reader"><Link href="/library" className="article-reader__back"><ChevronLeft size={18} aria-hidden="true" />Back to Library</Link><header><span>{categoryLabels[article.category]}</span><h1>{article.title}</h1><p>{article.summary}</p><div><small>{article.ageRange}</small><small><Clock3 size={14} aria-hidden="true" />{article.readingTime}</small><BookmarkButton article={article} active={isBookmarked(article.slug)} onClick={() => toggleBookmark(article.slug)} /></div></header><div className="article-reader__hero"><PiluIllustration variant={article.illustration} alt="Gentle Pilu baby illustration" /></div><ArticleReviewLabel label={article.reviewLabel} />{article.sections.map((section) => <ArticleSectionRenderer key={section.heading} section={section} />)}<KeyTakeawaysCard takeaways={article.keyTakeaways} /><ProfessionalHelpCard text={article.professionalHelp} /><p className="article-reader__disclaimer">Pilu Library provides general educational information and does not replace advice from a qualified healthcare professional.</p><section className="article-feedback"><p>Was this helpful?</p><div><button type="button" className={feedback[article.slug] === "yes" ? "article-feedback__button article-feedback__button--active" : "article-feedback__button"} onClick={() => setFeedback(article.slug, "yes")}>Yes</button><button type="button" className={feedback[article.slug] === "notReally" ? "article-feedback__button article-feedback__button--active" : "article-feedback__button"} onClick={() => setFeedback(article.slug, "notReally")}>Not really</button></div></section><AskPiluFromArticleButton question={question} /><section className="library-section"><header><div><p>Continue gently</p><h2>Related articles</h2></div></header><ArticleGrid articles={related} isBookmarked={isBookmarked} onBookmark={toggleBookmark} /></section><small className="article-reader__sources">{article.sourcesPlaceholder}</small></article>; }
export function ArticleSectionRenderer({ section }: { section: Article["sections"][number] }) { return <section className="article-content-section"><h2>{section.heading}</h2><p>{section.body}</p>{section.tips ? <ul>{section.tips.map((tip) => <li key={tip}>{tip}</li>)}</ul> : null}</section>; }
export function KeyTakeawaysCard({ takeaways }: { takeaways: string[] }) { return <section className="key-takeaways"><div><Sparkles size={18} aria-hidden="true" /><h2>Key takeaways</h2></div><ul>{takeaways.map((takeaway) => <li key={takeaway}>{takeaway}</li>)}</ul></section>; }
export function ProfessionalHelpCard({ text }: { text?: string }) { return <section className="professional-help"><Stethoscope size={19} aria-hidden="true" /><div><h2>When to contact a professional</h2><p>{text}</p></div></section>; }
export function ArticleReviewLabel({ label }: { label: string }) { return <p className="article-review-label"><ShieldCheck size={16} aria-hidden="true" />{label}</p>; }
export function AskPiluFromArticleButton({ question }: { question: string }) { return <Link className="ask-pilu-from-article" href={`/ask-pilu?question=${encodeURIComponent(question)}`}><Sparkles size={18} aria-hidden="true" /><span><b>Ask Pilu about this topic</b><small>Your question will be ready to review before sending.</small></span></Link>; }
