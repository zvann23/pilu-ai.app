"use client";
import { firstAidCategories, firstAidGuides } from "@/lib/first-aid-data";
import { AlertTriangle, Phone, Search, Settings } from "lucide-react";
import Link from "next/link"; import { useMemo, useState } from "react";
export function FirstAidHome() { const [query,setQuery]=useState(""); const guides=useMemo(()=>firstAidGuides.filter((g)=>`${g.title} ${g.summary} ${g.category} ${g.aliases.join(" ")}`.toLowerCase().includes(query.toLowerCase())),[query]); return <main className="first-aid"><header><p>First Aid</p><h1>Calm steps for urgent moments</h1></header><section className="first-aid__emergency" role="alert"><AlertTriangle aria-hidden="true"/><div><strong>Immediate danger</strong><p>If your child is unconscious, not breathing normally, having a severe allergic reaction, turning blue, experiencing a prolonged seizure or is in immediate danger, contact emergency services now.</p><div><Link href="/settings/emergency-contacts"><Phone/>Call local emergency services</Link><a href="#emergency-signs">View emergency signs</a></div></div></section><p className="first-aid__notice">Pilu provides general first-aid education. It cannot assess your child or replace emergency services or a qualified healthcare professional.</p><p className="first-aid__demo">Editorial demo content — professional medical review required before public release.</p><label className="first-aid__search"><Search/><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Search choking, fever, swallowed something…" aria-label="Search First Aid guides"/></label><section id="emergency-signs"><h2>Quick access</h2><div className="first-aid__grid">{guides.map((guide)=><Link key={guide.slug} href={`/first-aid/${guide.slug}`}><span>{guide.urgency.replaceAll("-"," ")}</span><strong>{guide.title}</strong><small>{guide.summary}</small></Link>)}</div></section><section><h2>Topics</h2><div className="first-aid__categories">{firstAidCategories.map((c)=><span key={c.slug}>{c.title}</span>)}</div></section><Link className="first-aid__settings" href="/settings/emergency-contacts"><Settings/>Configure emergency contacts</Link></main>; }
